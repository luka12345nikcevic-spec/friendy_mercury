model:
  name: rtdetr
  weights: Default
  score_threshold: 0.5

optimizer:
  name: AdamW
  lr0: 0.0001
  lrf: 0.01
  momentum: 0.9
  weight_decay: 0.0001

scheduler:
  cos_lr: true
  warmup_epochs: 1.0

loss:
  weights: {}
